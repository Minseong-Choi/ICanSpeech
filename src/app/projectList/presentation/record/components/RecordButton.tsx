"use client";

export default function RecordButton() {
  return (
    <button
      style={{
        padding: "12px 24px",
        fontSize: 16,
        fontWeight: "bold",
        backgroundColor: "#e63946",
        color: "#fff",
        border: "none",
        borderRadius: 8,
        cursor: "pointer",
      }}
    >
      ⏺️ 녹화 시작
    </button>
  );
}
